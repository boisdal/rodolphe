'use strict';

describe('myApp.jukebox module', function() {

  beforeEach(module('myApp.jukebox'));

  describe('jukebox controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});